<div class="order-page-header">
    <div>
        <h1>{{title}}</h1>
        {{#if description}}
            <p>{{description}}</p>
        {{/if}}
    </div>
    {{#if showSteps}}
        {{> dat-hang/common/checkout-steps activeStep=activeStep}}
    {{/if}}
</div>
