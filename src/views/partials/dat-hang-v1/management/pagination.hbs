<span class="order-pagination__info">{{#if total}}Hiển thị {{from}}–{{to}} trong {{total}} {{noun}}{{else}}Chưa có {{noun}}{{/if}}</span><div class="order-pagination__buttons"><button type="button" data-page="{{previous}}" {{#unless hasPrevious}}disabled{{/unless}} aria-label="Trang trước">‹</button>{{#each pages}}{{#if gap}}<span>…</span>{{else}}<button type="button" data-page="{{value}}" class="{{#if active}}is-active{{/if}}" {{#if active}}aria-current="page"{{/if}}>{{value}}</button>{{/if}}{{/each}}<button type="button" data-page="{{next}}" {{#unless hasNext}}disabled{{/unless}} aria-label="Trang sau">›</button></div>
